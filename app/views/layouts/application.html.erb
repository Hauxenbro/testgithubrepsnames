<!DOCTYPE html>
<html>
<head>
  <title>Githubrepsnames</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>

<body>
  <% flash[:alert].present? %>
    <p><%= flash[:alert] %></p>
  <%= yield %>
</body>
</html>
